using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using Sorting; 
using System.Linq;

namespace SortingTest
{
    [TestClass]
    public class Test
    {
        [TestMethod]
        public void BubbleSort_text()
        {
            //Arrange
            Random random = new Random();
            //int[] array = new int[sortArray.Length];
            //SortingArray sortArray= new SortingArray(10000, random);
            //Array.Copy(sortArray, temp, sortArray.array.Length);
            //Act
            //sortArray.bubbleSort(temp);
            //Assert
            //
            //Assert.IsTrue(Enumerable.SequenceEqual(temp,sortArray.arrayCreciente));

        }
        [TestMethod]
        public void QuickSort_text()
        {
            //Arrange
            Random random = new Random();
            //SortingArray sortArray= new SortingArray(10000, random);
            //int[] array = new int[sortArray.Length];
            //Array.Copy(sortArray, temp, sortArray.array.Length);
            //Act
            //sortArray.QuickSort(temp);
            //Assert
            //
            //Assert.IsTrue(Enumerable.SequenceEqual(temp,sortArray.arrayCreciente));

        }

    }

   
}
